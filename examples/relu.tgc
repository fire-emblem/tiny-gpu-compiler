// relu.tgc — ReLU activation function
// Tests conditional branching (if/else)

kernel relu(global int* output, global int* input) {
    int idx = blockIdx * blockDim + threadIdx;
    int val = input[idx];
    if (val > 0) {
        output[idx] = val;
    } else {
        output[idx] = 0;
    }
}
